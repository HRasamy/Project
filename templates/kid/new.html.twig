{% extends 'base.html.twig' %}

{% block body %}
    <section class="new_kid">
        <h1>Infos de l'enfant</h1>
            <div class="form">
                {{ form_start(form) }}
                {{form_widget(form)}}

                <button type="submit" class="btn btn-primary mb-3">Valider</button>
                {{ form_end(form) }}
            </div>
    </section>
{% endblock %}

